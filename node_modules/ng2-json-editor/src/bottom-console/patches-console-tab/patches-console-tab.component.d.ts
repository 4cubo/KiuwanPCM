import { OnInit, ChangeDetectorRef } from '@angular/core';
import { AbstractSubscriberComponent } from '../../abstract-subscriber';
import { DomUtilService, PathUtilService, JsonStoreService } from '../../shared/services';
import { JsonPatch } from '../../shared/interfaces';
export declare class PatchesConsoleTabComponent extends AbstractSubscriberComponent implements OnInit {
    private domUtilService;
    private pathUtilService;
    private jsonStoreService;
    private changeDetectorRef;
    patches: Array<JsonPatch>;
    constructor(domUtilService: DomUtilService, pathUtilService: PathUtilService, jsonStoreService: JsonStoreService, changeDetectorRef: ChangeDetectorRef);
    ngOnInit(): void;
    focusPatch(patch: JsonPatch): void;
    acceptAll(): void;
    rejectAll(): void;
}
